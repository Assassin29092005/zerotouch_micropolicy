{
  "name": "zerotouch-micropolicy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npx serve frontend/",
    "deploy-functions": "npx supabase functions deploy admin --no-verify-jwt --project-ref YOUR_SUPABASE_PROJECT_ID"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "supabase": "^1.182.1"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1"
  }
}
